#!/usr/bin/bash

PROJECT_NAME=project
BUILD_VERBOSE=OFF
source $TOOLCHAIN_PATH/build.in

if [[ $1 == "command" ]]; then
    echo "project specific command"

else
     echo "ERROR: unknown command"
fi
